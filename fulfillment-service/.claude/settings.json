{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(cat - | jq -r '.tool_input.file_path // .tool_input.filePath // empty') && case \"$file_path\" in *.proto) cd \"$CLAUDE_PROJECT_DIR\" && uv run dev.py lint proto && buf generate ;; *go.mod) cd \"$CLAUDE_PROJECT_DIR\" && go mod tidy ;; esac"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(cat - | jq -r '.tool_input.command // empty') && case \"$cmd\" in *'git commit'*) cd \"$CLAUDE_PROJECT_DIR\" && uv run dev.py lint proto ;; *'gh pr create'*) cd \"$CLAUDE_PROJECT_DIR\" && gofmt -s -w . && test -z \\\"$(git diff --name-only)\\\" && uv run dev.py lint proto && ginkgo run -r internal ;; esac"
          }
        ]
      }
    ]
  }
}
