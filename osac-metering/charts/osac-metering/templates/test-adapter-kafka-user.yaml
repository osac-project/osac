{{- if .Values.testAdapter.enabled }}
apiVersion: kafka.strimzi.io/v1
kind: KafkaUser
metadata:
  name: osac-metering-test-adapter
  namespace: {{ include "osac-metering.kafkaClusterNamespace" . }}
  labels:
    strimzi.io/cluster: {{ include "osac-metering.kafkaClusterName" . }}
spec:
  authentication:
    type: scram-sha-512
  authorization:
    type: simple
    acls:
      - resource:
          type: topic
          name: osac.metering.
          patternType: prefix
        operations:
          - Read
          - Describe
        host: "*"
      - resource:
          type: group
          name: osac-metering-test-adapter
          patternType: literal
        operations:
          - Read
        host: "*"
{{- end }}
