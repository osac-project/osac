---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: fabricdomains.osac.openshift.io
spec:
  group: osac.openshift.io
  names:
    kind: FabricDomain
    listKind: FabricDomainList
    plural: fabricdomains
    shortNames:
    - fd
    singular: fabricdomain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.servers
      name: Servers
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FabricDomain is the Schema for the fabricdomains API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of FabricDomain.
            properties:
              servers:
                description: Servers lists the hostnames of the servers that belong
                  to the fabric domain.
                items:
                  minLength: 1
                  type: string
                minItems: 1
                type: array
              type:
                description: Type is the physical fabric type. It cannot be changed
                  after creation.
                enum:
                - EthernetEW
                - InfiniBandEW
                - NVLink
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
              virtualNetwork:
                description: |-
                  VirtualNetwork is the ID of the OSAC VirtualNetwork associated with the domain.
                  For Netris EthernetEW, this VirtualNetwork is the source of truth for the VPC where the Server
                  Cluster is created; its NetworkClass supplies backend configuration such as the cluster template.
                  InfiniBandEW and NVLink are reserved for later phases and may define different connectivity.
                  This reference is immutable after creation.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: virtualNetwork is immutable
                  rule: self == oldSelf
            required:
            - servers
            - type
            - virtualNetwork
            type: object
          status:
            description: status defines the observed state of FabricDomain.
            properties:
              backendId:
                description: BackendID is the provider-specific identifier of the
                  server cluster.
                type: string
              conditions:
                description: Conditions describes the current state of the FabricDomain.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              members:
                description: Members contains the observed state of each server in
                  the domain.
                items:
                  description: FabricDomainMemberStatus describes the observed state
                    of one server in a fabric domain.
                  properties:
                    message:
                      description: Message contains additional information about the
                        server state.
                      type: string
                    server:
                      description: Server is the hostname of the server.
                      type: string
                    state:
                      description: State is the provisioning state of the server.
                      enum:
                      - Pending
                      - Active
                      - Failed
                      type: string
                  required:
                  - server
                  type: object
                type: array
              vpcId:
                description: VPCID is the provider-specific identifier of the VPC
                  containing the fabric domain.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
