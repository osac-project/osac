---
argument_specs:
  create:
    options:
      bare_metal_pool:
        type: dict
        required: true
        description: BareMetalPool CR from fulfillment-api via osac_job_vars
  delete:
    options:
      bare_metal_pool:
        type: dict
        required: true
        description: BareMetalPool CR from fulfillment-api via osac_job_vars
