{{- if .Values.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cost-management-adapter.fullname" . }}
  labels:
    {{- include "cost-management-adapter.labels" . | nindent 4 }}
{{- end }}
